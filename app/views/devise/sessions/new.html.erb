<% unless @school_informations.nil? %>
  <div class="middle-box loginscreen">
    <div>
      <div>
        <img src="<%= @school_informations&.logo&.attached? ? url_for(@school_informations.logo) : (asset_pack_path "static/images/logo_elvis_a.png") %>" width="150" height="150" class="picto-loginscreen"/>
      </div>
    </div>
  </div>
<% end %>

<div class="loginscreen-iboxes">
  <div class="row">
    <div class="col-lg-6">
      <div class="ibox" >
        <div class="ibox-title">
          <h5><%= t("views.devise.sessions.new.title") %></h5>
        </div>
        <div class="ibox-content">
          <%= form_for(resource, as: resource_name, url: session_path(resource_name), class: "m-t") do |f| %>
            <div class="form-group">
              <%= f.label t("views.devise.sessions.new.login_label") %><br />
              <%= f.text_field :login, required: true, autofocus: true, class: "form-control" %>
              <% if resource.errors.any? %>
                <div class="alert alert-danger">
                  <% resource.errors.full_messages.each do |error| %>
                    <p><%= error %></p>
                  <% end %>
                </div>
              <% end %>
            </div>

            <div class="form-group">
              <%= react_component('PasswordInput', { id: 'password', name: 'user[password]', label: t("activerecord.attributes.user.password") }) %>
            </div>

            <% if devise_mapping.rememberable? -%>
              <div class="form-group pull-left">
                <%= f.check_box :remember_me, class: "icheckbox_square-green" %>
                <%= f.label :remember_me, t("views.devise.sessions.new.remember_me") %>
              </div>
            <% end -%>

            <%= f.submit t("devise.sessions.new.sign_in"), class: "btn btn-primary block full-width m-b" %>

            <%= render "devise/shared/links" %>
          <% end %>
        </div>
      </div>
    </div>

    <% if devise_mapping.registerable? && controller_name != "registrations" %>
      <div class="col-lg-6">
        <div class="ibox" style="margin-bottom: 60px">
          <div class="ibox-title">
            <h5><%= t("views.devise.sessions.new.no_account_title") %></h5>
          </div>
          <div class="ibox-content text-center">
            <p class="m-t-md m-b-lg"><%= t("views.devise.sessions.new.no_account_body") %></p>
            <%= link_to t("devise.registrations.new.sign_up"), new_registration_path(resource_name), class: "btn btn-primary block full-width" %>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
