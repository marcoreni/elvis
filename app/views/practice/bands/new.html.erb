<h1><%= t("views.practice.bands.new.title") %></h1>

<%= react_component('practice/BandCreator', {
    bandTypes: @band_types,
    musicGenres: @music_genres,
    instruments: @instruments,
    season: Season.current_apps_season,
}) %>

<%= link_to t("common.actions.back"), parameters_practice_parameters_path + "#tab-2", class: 'btn btn-primary' %>
