<%= form_with(model: [:practice, music_genre], local: true) do |form| %>

    <% if music_genre.errors.any? %>
        <div id="error_explanation">
            <h2><%= t("views.shared.form_errors.heading", count: music_genre.errors.count) %></h2>
            <ul>
                <% music_genre.errors.full_messages.each do |message| %>
                <li><%= message %></li>
                <% end %>
            </ul>
        </div>
    <% end %>

    <div class="row">
        <div class="field form-group col-lg-3 col-md-4 col-sm-12">
            <%= form.label :name %>
            <%= form.text_field :name, :class => 'form-control' %>
        </div>
    </div>

    <div class="actions">
        <%= link_to t("common.actions.back"), parameters_practice_parameters_path + "#tab-1", :class => 'btn btn-primary' %>
        <%= form.submit t("common.actions.save"), :class => 'btn btn-primary' %>
    </div>

<% end %>
